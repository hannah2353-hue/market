# OK Market Intelligence · Deployment

🔴 **Internal use only** — Do not share the URL publicly.

## 🔑 Login

비밀번호: *(내부 공유 채널에서만 배포)*

## 🚀 Access

배포된 URL을 브라우저에 입력 → 로그인 → 대시보드.

## 🛡 Security Notes

- 데이터는 `AES-GCM + PBKDF2 100k` 암호화
- 비밀번호 없이는 내용 복호화 불가
- 검색엔진 색인 차단 (`robots.txt` + `noindex` meta)
- **그래도 URL·파일이 외부 유출될 경우 오프라인 brute force 위험** → 비밀번호는 내부 인원에게만
